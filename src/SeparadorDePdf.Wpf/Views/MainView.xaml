<Window x:Class="SeparadorDePdf.Wpf.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:vm="clr-namespace:SeparadorDePdf.Wpf.ViewModels"
    Title="Separador de PDF - OCR Automático"
    Width="900" Height="700" MinWidth="700" MinHeight="550"
    WindowStartupLocation="CenterScreen"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    Background="{DynamicResource MaterialDesignPaper}"
    TextElement.FontWeight="Medium"
    TextElement.FontSize="14"
    FontFamily="Segoe UI">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:InvertBoolConverter x:Key="InvertBoolConverter" xmlns:local="clr-namespace:SeparadorDePdf.Wpf.Converters"/>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Grid.Row="0" Margin="0,0,0,16" Padding="16">
            <StackPanel>
                <TextBlock Text="SEPARADOR DE PDF" FontSize="24" FontWeight="Bold"/>
                <TextBlock Text="Organização automática com OCR de documentos escaneados" FontSize="14" Opacity="0.7"/>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="1" Margin="0,0,0,12" Padding="16">
            <StackPanel>
                <TextBlock Text="Configuração" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <Grid Margin="0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Arquivo PDF:" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding InputFilePath}" Margin="8,0" IsReadOnly="True" materialDesign:HintAssist.Hint="Selecione o PDF escaneado"/>
                    <Button Grid.Column="2" Content="Selecionar" Command="{Binding BrowseInputFileCommand}" Margin="4,0"/>
                </Grid>
                <Grid Margin="0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Pasta Destino:" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding OutputFolder}" Margin="8,0" IsReadOnly="True" materialDesign:HintAssist.Hint="Pasta de saída organizada"/>
                    <Button Grid.Column="2" Content="Procurar" Command="{Binding BrowseOutputFolderCommand}" Margin="4,0"/>
                </Grid>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="2" Margin="0,0,0,12" Padding="16">
            <StackPanel Orientation="Horizontal">
                <Button Content="▶ Iniciar" Command="{Binding StartProcessingCommand}" Margin="0,0,8,0" IsEnabled="{Binding IsProcessing, Converter={StaticResource InvertBoolConverter}}"/>
                <Button Content="■ Parar" Command="{Binding StopProcessingCommand}" Margin="0,0,8,0" IsEnabled="{Binding IsProcessing}"/>
                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" Margin="16,0,0,0" FontWeight="SemiBold" Opacity="0.8"/>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="3" Margin="0,0,0,12" Padding="16">
            <StackPanel>
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding CurrentFile, StringFormat='Processando: {0}'}" FontSize="13" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Grid.Column="1" Text="{Binding ProgressPercentage, StringFormat='{}{0:F0}%'}" FontWeight="Bold" Margin="8,0,0,0"/>
                </Grid>
                <ProgressBar Value="{Binding ProgressPercentage}" Minimum="0" Maximum="100" Height="20"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <materialDesign:PackIcon Kind="CheckCircle" Foreground="Green" Margin="0,0,4,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding SuccessCount, StringFormat='Sucesso: {0}'}" Margin="0,0,24,0"/>
                    <materialDesign:PackIcon Kind="AlertCircle" Foreground="Red" Margin="0,0,4,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ErrorCount, StringFormat='Erros: {0}'}"/>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="4" Padding="8">
            <DockPanel>
                <TextBlock Text="Logs em Tempo Real" FontSize="13" FontWeight="SemiBold" DockPanel.Dock="Top" Margin="8,4"/>
                <ListBox ItemsSource="{Binding Logs}" DockPanel.Dock="Top" Margin="0,4,0,0" ScrollViewer.HorizontalScrollBarVisibility="Auto" x:Name="LogList">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}'}" Margin="0,0,8,0" FontWeight="Bold" Opacity="0.6"/>
                                <TextBlock Grid.Column="1" Text="{Binding Level}" Margin="0,0,8,0" FontWeight="Bold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                                    <Setter Property="Foreground" Value="Red"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                    <Setter Property="Foreground" Value="Orange"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Info">
                                                    <Setter Property="Foreground" Value="Green"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Grid.Column="2" Text="{Binding Message}" TextWrapping="Wrap"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </materialDesign:Card>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Exportar Logs" Command="{Binding ExportLogsCommand}" Margin="0,0,8,0"/>
            <Button Content="Limpar Histórico" Command="{Binding ClearHistoryCommand}"/>
        </StackPanel>
    </Grid>
</Window>