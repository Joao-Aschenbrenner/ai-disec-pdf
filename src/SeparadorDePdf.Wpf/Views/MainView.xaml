<Window x:Class="SeparadorDePdf.Wpf.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:SeparadorDePdf.Wpf.Converters"
    Title="Separador de PDF - OCR Automático"
    Width="1100" Height="750" MinWidth="850" MinHeight="600"
    WindowStartupLocation="CenterScreen"
    Background="#F5F5F5"
    FontFamily="Segoe UI">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:InvertBoolConverter x:Key="InvertBoolConverter"/>
        <local:InvertVisibilityConverter x:Key="InvertVisConverter"/>

        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="2" Opacity="0.12" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#1A237E"/>
        </Style>

        <Style x:Key="SubtitleText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#666"/>
        </Style>

        <Style x:Key="SectionText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#888"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="GreenButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#43A047"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="RedButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#E53935"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#3949AB" Padding="24,20">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📄" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel>
                        <TextBlock Text="SEPARADOR DE PDF" FontSize="20" FontWeight="Bold" Foreground="White"/>
                        <TextBlock Text="Organização automática com OCR" FontSize="12" Foreground="#CCFFFFFF"/>
                    </StackPanel>
                </StackPanel>
                <TextBlock Text="{Binding StatusText}" FontSize="14" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Right"
                           Visibility="{Binding IsProcessing, Converter={StaticResource InvertVisConverter}}"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <Grid Margin="0,0,12,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Style="{StaticResource CardBorder}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="CONFIGURAÇÃO" Style="{StaticResource SectionText}"/>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding InputFilePath}" IsReadOnly="True" Padding="12,10" FontSize="13"
                                         Background="#FAFAFA" BorderBrush="#E0E0E0" Margin="0,0,8,0"/>
                                <Button Grid.Column="1" Content="Selecionar" Command="{Binding BrowseInputFileCommand}"
                                        Background="#E8EAF6" Foreground="#3949AB" Padding="16,8" FontWeight="Medium"/>
                            </Grid>
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding OutputFolder}" IsReadOnly="True" Padding="12,10" FontSize="13"
                                         Background="#FAFAFA" BorderBrush="#E0E0E0" Margin="0,0,8,0"/>
                                <Button Grid.Column="1" Content="Procurar" Command="{Binding BrowseOutputFolderCommand}"
                                        Background="#E8EAF6" Foreground="#3949AB" Padding="16,8" FontWeight="Medium"/>
                            </Grid>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Style="{StaticResource GreenButton}" Command="{Binding StartProcessingCommand}"
                                Content="▶ INICIAR" IsEnabled="{Binding IsProcessing, Converter={StaticResource InvertBoolConverter}}"/>
                        <Button Grid.Column="1" Style="{StaticResource RedButton}" Content="■ PARAR"
                                Command="{Binding StopProcessingCommand}" IsEnabled="{Binding IsProcessing}" Margin="8,0"/>

                        <ProgressBar Grid.Column="2" Value="{Binding ProgressPercentage}" Minimum="0" Maximum="100"
                                     Width="40" Height="40" Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVis}}"
                                     Style="{StaticResource {x:Type ProgressBar}}"/>
                    </Grid>
                </Border>

                <Border Grid.Row="2" Style="{StaticResource CardBorder}"
                        Visibility="{Binding IsProcessing, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock Text="PROGRESSO" Style="{StaticResource SectionText}"/>
                        <TextBlock Text="{Binding CurrentFile}" FontSize="13" TextTrimming="CharacterEllipsis" Margin="0,0,0,12"/>
                        <ProgressBar x:Name="ProgressBarControl" Value="{Binding ProgressPercentage}" Minimum="0" Maximum="100" Height="8"
                                     Background="#E0E0E0" Foreground="#3949AB"/>
                        <TextBlock Text="{Binding ProgressPercentage, StringFormat={}{0:F0}% concluído}" FontSize="12" Foreground="#666" Margin="0,8,0,12"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="✓" Foreground="#43A047" FontWeight="Bold"/>
                                <TextBlock Text="{Binding SuccessCount, StringFormat={}{0} sucesso}" FontSize="14" Margin="4,0,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <TextBlock Text="✗" Foreground="#E53935" FontWeight="Bold"/>
                                <TextBlock Text="{Binding ErrorCount, StringFormat={}{0} erros}" FontSize="14" Margin="4,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Row="2" Style="{StaticResource CardBorder}"
                        Visibility="{Binding IsProcessing, Converter={StaticResource InvertVisConverter}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="☁️" FontSize="48" HorizontalAlignment="Center" Opacity="0.3"/>
                        <TextBlock Text="Selecione um PDF e pasta de destino" FontSize="15" HorizontalAlignment="Center" Margin="0,12,0,0" Opacity="0.5"/>
                        <TextBlock Text="Depois clique em INICIAR" FontSize="13" HorizontalAlignment="Center" Margin="0,4,0,0" Opacity="0.3"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Column="1" Style="{StaticResource CardBorder}">
                <DockPanel>
                    <Grid DockPanel.Dock="Top">
                        <TextBlock Text="LOGS" Style="{StaticResource SectionText}" Margin="0,0,0,8"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Command="{Binding ExportLogsCommand}" Content="Exportar" Padding="8,4" FontSize="12"
                                    Background="Transparent" Foreground="#666" BorderThickness="0" Margin="0,0,4,0"/>
                            <Button Command="{Binding ClearHistoryCommand}" Content="Limpar" Padding="8,4" FontSize="12"
                                    Background="Transparent" Foreground="#666" BorderThickness="0"/>
                        </StackPanel>
                    </Grid>
                    <ListBox ItemsSource="{Binding Logs}" BorderThickness="0" Background="Transparent"
                             x:Name="LogList" VirtualizingPanel.ScrollUnit="Pixel">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,2"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8,6" CornerRadius="6" Background="#FAFAFA">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="55"/>
                                            <ColumnDefinition Width="50"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Timestamp, StringFormat={}{0:HH:mm:ss}}"
                                                   FontFamily="Consolas" FontSize="11" Opacity="0.5"/>
                                        <Border Grid.Column="1" CornerRadius="4" Padding="4,2" HorizontalAlignment="Left">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#E8F5E9"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Level}" Value="Error">
                                                            <Setter Property="Background" Value="#FFEBEE"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                            <Setter Property="Background" Value="#FFF3E0"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Debug">
                                                            <Setter Property="Background" Value="#E3F2FD"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding Level}" FontSize="10" FontWeight="Bold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#2E7D32"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Level}" Value="Error">
                                                                <Setter Property="Foreground" Value="#C62828"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                                <Setter Property="Foreground" Value="#EF6C00"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Level}" Value="Debug">
                                                                <Setter Property="Foreground" Value="#1565C0"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                        <TextBlock Grid.Column="2" Text="{Binding Message}" TextWrapping="Wrap" FontSize="12" Margin="8,0,0,0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</Window>